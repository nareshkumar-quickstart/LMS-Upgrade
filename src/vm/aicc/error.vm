<html>
	
<body>
#if($context.aicc_sid)
aicc_sid parameter missing
<br/>
#end

#if($context.aicc_url)
aicc_url parameter missing
<br/>
#end

#if($context.course_id)
course_id parameter missing
<br/>
#end

#if($context.customer_id)
customer_id parameter missing
<br/>
#end

#if($context.invalid_customer)
invalid customer_id provided
<br/>
#end

#if($context.aicc_disabled)
the AICC interface is not enabled
<br/>
#end

#if($context.invalid_course)
invalid course_id provided
<br/>
#end

#if($context.error_getparam)
error during getparam communication with LMS 
#end

#if($context.studentid_not_found)
student id not found during getparam communication with LMS 
#end

#if($context.studentname_not_found)
student name not found during getparam communication with LMS 
#end

#if($context.error_searchUser)
 <br>User could Not be found.$context.error_searchUser<br>
#end	

#if($context.enrollment_error)
 <br>An error occured.$context.enrollment_error_text<br>
#end
</body>
</html>