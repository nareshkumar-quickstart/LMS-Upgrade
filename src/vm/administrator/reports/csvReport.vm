#set($count=1)#set($size=$columns.size())#foreach($col in $sorter.sort($columns, "displayOrder"))"$col.displayName#if($count < $size)",#set($count=$count+1)#else"#end#end#foreach($item in $objects)

#set($count=1)#set($size=$columns.size())#foreach($col in $sorter.sort($columns, "displayOrder"))"$formUtil.formatReportField($col.columnFormat, $item.get($col.dbColumnName))#if($count < $size)",#set($count=$count+1)#else"#end#end #end